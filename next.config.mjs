/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  assetPrefix: "/tap-tap/",
  basePath: "/tap-tap",
images: {
    unoptimized: true,
  },
}

export default nextConfig
